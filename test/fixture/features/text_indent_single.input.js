//This comment has no space between the // and the text. This should be preserved after the text is wrapped.

// This comment has 1 space between the // and the text. It should be preserved after the text is wrapped.

//  This comment has 2 spaces between the // and the text, because the first line of text has 2 spaces, following lines that are created will have the same.

// This comment text has a one space indent.
// This second line will be appended to the first, and the following line will follow the indent of the first.

// Lines that are indented two spaces or more further than the first line aren't touched.
// This is for code examples etc. Example:
//
//   // Use it like this
//   Files* GetSecretFiles() {
//       return TheSystem.accessFiles("all the secret files");
//   }
//