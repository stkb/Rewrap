/** A jsdoc-style ^comment on a single line.^ When this gets wrapped, new lines will have a '*' prefix added, that lines up with the first '*' of the first line, but the text will be indented one character less than the first line. */